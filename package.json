{
  "name": "weare",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @weare/web",
    "build": "npm run build -w @weare/web"
  }
}
